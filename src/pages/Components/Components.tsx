import React from "react";

import styles from "./Components.module.scss";

const Components: React.FC = () => {
  return <div className={styles.components}>components</div>;
};

export default Components;
